{% extends 'base.html.twig' %}

{% block title %}Eddy's Admin | Styles{% endblock %}

{% block body %}


    <section class="white-wrapper">
        <div class="section-inner">
            <div class="container">
                <div class="row mb60 text-center">
                    <div class="col-sm-12">
                        <h3 class="section-title">Tous les styles</h3>
                    </div>
                </div>
            </div>
            <div class="container">
                <div class="row mb40">

                    <div class="col-sm-4 col-sm-offset-4">
                        <div class="text-center">        
                            <a class="btn btn-primary mt30 btn-lg btn-block" href={{"/zone/admin/styles/creation"}}>Add Style</a>
                        </div>
                        <br>
                        <br>
                        <br>
                    </div>


                    <div class="container">
                        <div class="row mb40">

                            <table class="table  table-striped">
                                <thead style="background-color: #333; color: white">
                                    <tr>
                                        <th scope="col">Id</th>
                                        <th scope="col">Type de Style</th>
                                        <th scope="col"></th>
                                    </tr>
                                </thead>

                                <tbody>

                                    {% for valeur in lesstyles|reverse %}

                                        <tr>
                                            <th scope="row">{{valeur.id}} </th>
                                            <td>{{valeur.typestyle}} </td>
                                            <td>        
                                                <a class="btn" href="/zone/admin/styles/one/{{valeur.id}}">Détails</a>
                                            </td>
                                        </tr>                           
                                    {% endfor %}

                                </tbody>
                            </table>
                        </div>
                    </div>

                </div>
            </div>
        </div>
    </section>

    {#
                        {% for valeur in lesstyles|reverse %}
                        <ul>
                            <li>{{valeur.id}} </li>
                            <li>{{valeur.typestyle}} </li>


                            <a class="btn" href="/zone/admin/styles/one/{{valeur.id}}">Modifier</a>
                        </ul>
                    {% endfor %}
    #}
{% endblock %}